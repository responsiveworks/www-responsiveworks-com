# Shopify App Configuration
# Get these values from your Shopify Partner Dashboard
# (Partners > Apps > Your App > Client credentials)

# Your app's API key (Client ID)
SHOPIFY_API_KEY=your_api_key_here

# Your app's API secret (Client secret)
SHOPIFY_API_SECRET=your_api_secret_here

# Comma-separated list of OAuth scopes your app needs
# See: https://shopify.dev/docs/api/usage/access-scopes
SHOPIFY_SCOPES=read_products,read_orders,read_customers,read_inventory

# Your app's public URL (used for OAuth redirect)
# In production, this should be your deployed domain
SHOPIFY_APP_URL=https://your-domain.com

# Alternative: Use NEXT_PUBLIC_APP_URL if you need it on the client side
# NEXT_PUBLIC_APP_URL=https://your-domain.com
